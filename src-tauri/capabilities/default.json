{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the ferric window is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
